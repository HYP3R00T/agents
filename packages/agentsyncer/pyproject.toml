[project]
name = "agentsyncer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [{ name = "Rajesh Das", email = "hyperoot.tech@proton.me" }]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
agentsyncer = "agentsyncer:main"

[build-system]
requires = ["uv_build>=0.11.6,<0.12.0"]
build-backend = "uv_build"
